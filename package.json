{
  "name": "medusa-store",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "cleanup:medusa": "cd medusa && npm run clean",
    "setup:medusa": "cd medusa && npm install",
    "setup:storefront": "cd storefront && npm install",
    "seed": "cd medusa && npm run seed",
    "dev:medusa": "cd medusa && npm run dev",
    "dev:storefront": "cd storefront && npm run dev",
    "dev": "concurrently --kill-others \"npm run dev:medusa\" \"npm run dev:storefront\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
