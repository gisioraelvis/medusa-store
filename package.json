{
  "name": "medusa-store",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "cleanup:medusa": "cd medusa && yarn clean",
    "setup:medusa": "cd medusa && yarn install",
    "setup:storefront": "cd storefront && yarn install",
    "seed": "cd medusa && yarn seed",
    "dev:medusa": "cd medusa && yarn dev",
    "dev:storefront": "cd storefront && yarn dev",
    "dev": "concurrently --kill-others \"yarn dev:medusa\" \"yarn dev:storefront\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
